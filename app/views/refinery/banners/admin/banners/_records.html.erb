<%= render 'refinery/admin/search_header', :url => refinery.banners_admin_banners_path %>
<div class='pagination_container'>
  <% if @banners.any? %>
    <%= render 'banners' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
